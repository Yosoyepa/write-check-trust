# Revisión E11 — diff completo de la PR #53 y aptitud para merge (2026-09-17)

Informe final de la revisión E11. Tres revisores independientes de solo
lectura (A: producto/arquitectura; B: contratos/evidencia; C:
gobernanza/integración) más el coordinador que resuelve contradicciones.
Esta revisión no repara hallazgos, no ejecuta campañas nuevas ni full, no
hace bless, no saca la PR de borrador, no hace merge/bump/tag/release, y no
reabre decisiones humanas válidas. **No confunde aptitud para merge,
acreditación AC1 y cierre de beta.3.**

## 0. Identidades y preflight

| Elemento | Valor verificado |
|---|---|
| SHA auditado (SHA técnico final del incremento) | `32d3491b3a98543d7b13c933482d3ced4ac49a4b` = HEAD local = `origin` = headRefOid PR #53 (sin avance) |
| Base efectiva | `932c835ac0eeb75010ec7a1071315b1c51f78eb6` = baseRefOid de la PR = merge-base con `origin/main` (main no avanzó) |
| Candidato aprobado pre-bless | `903f96162ad349d8f4e9c4aacc721cd0397eadbd` |
| Commit de productos del bless | `1fc79157be0656fa18fbe410024d37121791236b` |
| Árbol | limpio al iniciar la revisión; único sin seguimiento el documento ajeno `PREPARACION-SALIDA-INCREMENTAL-BETA3.1-2026-09-13.md`, preservado. Al publicar: staging exclusivo de este informe y los hunks E11 de la matriz; el commit documental no toca producto ni premisas (verificación previa al commit) |
| PR | OPEN, **borrador**, MERGEABLE, `mergeStateStatus: CLEAN` (historial lineal requerido satisfecho); `reviewDecision` vacío (cero reviews) |
| Checks requeridos (branch protection de main) | único contexto requerido `commit-gates` → **PASS** en 32d3491 (run 35267270538 / job 105357436304, 4m2s); workflows `adoption-smoke` y `full-hardening` no corren en PRs |
| Identidades del diff | 107 archivos (+24895/−1671): 30 producto nuevo, 4 producto modificado, 3 dependencias/gobernanza de lint, 14 tests/features, 2 productos del bless, 54 documentación/evidencia |

## 1. Dictamen

### **APTO PARA MERGE**

No hay bloqueantes confirmados ni obligaciones exigibles pendientes para
integrar este incremento. Fundamento:

1. **Cero DEFECTOS CONFIRMADOS bloqueantes** en los tres frentes (§2).
2. **Puerta de merge del plan vigente satisfecha** (PLAN-EJECUTABLE §2
   POST-BLESS): G-META-1 verde (`wct integrity check` exit 0; tier commit
   21/21 PASS re-ejecutados sobre 32d3491) y gates pertinentes verdes; CI
   `commit-gates` PASS sobre el SHA auditado. Ningún requisito de
   repositorio (no humano) pendiente: branch protection solo exige
   `commit-gates`; sin reviews exigidas por configuración.
3. **Revisión protegida E9 cerrada** con bless humano verificado (delta
   +30/0, 7+log; commits posteriores solo documentales).
4. Las decisiones de merge y salida de borrador siguen siendo **humanas**;
   este dictamen no las ejecuta ni las sustituye.

Condición de identidad para que el dictamen conserve vigencia: el merge debe
realizarse sobre `32d3491` (identidad `M` del plan). Cualquier commit
posterior con producto/tests/config recalifica; los puramente documentales
deben identificarse y verificarse como este informe lo hace — el commit
documental que publica este informe no altera producto ni premisas (staging
exclusivo de docs, verificado antes del commit).

## 2. Cobertura de revisión y hallazgos por frente

### Frente A — producto y arquitectura (sin defectos confirmados)

- **Traslados y fachadas (TEST-007)**, más allá del AST: identidad REGISTRY
  por `is` (47 claves; `runner.gate_sast_semgrep is REGISTRY["G-SAST-SEMGREP"]
  is semgrep.gate_sast_semgrep` → True, sonda ejecutada); parches de
  monkeypatch stdlib (`runner.shutil is shutil`) siguen interceptando gates
  ahora residentes en submódulos (verificado empíricamente contra
  G-COV-TOTAL, G-DOC y el preflight git de G-SAST-SEMGREP); fachada
  fixtures 33/33 símbolos; sin ciclos. Las dos diferencias AST (`_declared`,
  `f9_a`) entraron en 88de60e (no en la partición) y están cubiertas por
  tests y mutación.
- **Motor de aceptación**: PASS vacuo bloqueado empíricamente (IR sin
  Examples/scenarios → bloqueo TEST-010; planned==0 nunca aprueba); error
  instrumental nunca es killed; transporte fail-closed (límite 1 MiB exacto,
  captura parcial persistida, `_close_streams` en finally, doble terminate
  guardado, TimeoutExpired residual → invalid); campaña sin herencia de
  `WCT_ACCEPT_*`, nonce fresco, baseline-primero, invalid para → resto
  `not_run`; recibo con `O_NOFOLLOW|O_NONBLOCK`, S_ISREG, 1 MiB+1,
  duplicados/NaN/claves extra rechazados; `SemanticMismatchError` con alias
  contractual.
- **Semgrep**: la reparación CRAP (aed2acf) no cambió comportamiento
  observable (comparación pre/post de `_python_files`/`_build_dirs`); el
  único cambio de comportamiento real es la reparación de targets (4217253),
  deliberada, documentada y con TDD. Frontera argv ~5 KB vs ARG_MAX 2 MB
  (~400× margen).
- **Regresiones de API/imports**: ninguna (todos los imports reales de las
  tres fachadas resuelven; colección 696 OK).
- Hallazgos: **MEJORA NO BLOQUEANTE** `tools/wct/accept/verdict.py:42-43`
  (lectura de `ir["scenarios"]`/escritura de `vacuous` antes del `try`; IR
  malformado lanzaría `KeyError` fuera del manejador — no alcanzable por
  consumidores reales, que usan `parse_feature`; reparación mínima: mover
  dos líneas dentro del `try`); cosméticos: join vacío del mensaje vacuous y
  `command` display con espacios potenciales (el binding compara argv real).

### Frente B — contratos y evidencia (conciliación consistente)

- **Brutos conciliados por identidad en todas las capas**: r2 498=468+30;
  r2b 486=462+24 (24 ratificados); r3 497=450+47 (47=14A+14B+7C+11
  defectos); GS-1 282=280+2; GS-2 102+16 (sucesiva 118); GS-3 108=74+34
  (sensibilidad 27/27; 7 equivalencias ratificadas; corte `eec71fb1`,
  fuente `0126c4e1`, digest `63c83bd2`); fase E 27+4 (micro 4; recampaña
  31+0; **928 no ejecutados delimitados**); cleanup 8=6+2 ratificados;
  Q-ACCMUT 47/47 (repetición fresca por cambio de `process.py`);
  SAST 74=69+5 (5 sucesoras ratificadas D-A' por diff exacto).
- **Anclas re medidas en HEAD por el revisor**: `semgrep.py` `33ff0509`,
  `process.py` `1a25984b`, `campaign.py` `686070cd`,
  `pytest_receipt.py` `d4f136ae` y los 8 anclajes de Q-ACCMUT — 8/8
  idénticos en 32d3491.
- **TEST-003/TEST-010**: ejemplos verificados con aserciones sobre el SUT
  (campañas reales con subprocesos, semgrep real); Outlines parametrizados
  completos; caracterización etiquetada con honestidad.
- Hallazgos:
  - **H1 (DEFECTO CONFIRMADO, documental, no bloqueante)**: filas A3 y C3 de
    la matriz sin anotación de «Registro final», desincronizadas de
    `DISPOSICION-FINAL` y `ACTA-DECISIONES-FINALES` (que las cerraron como
    defectos con regresión permanente). Reparación mínima: hunks de matriz.
  - **H2 (DEFECTO CONFIRMADO, documental, no invalidante)**: las
    ratificaciones A12/A13/terminate__7 ancladas a `process.py 84b9340f…`
    sin nota de re-anclaje tras dos cambios de fuente; el revisor verificó
    por diff propio que `_read`/`_observe`/`_terminate` son byte-idénticos
    entre `84b9340f` y `1a25984b` → las equivalencias son fácticamente
    transferibles; falta la nota de vigencia. Reparación mínima: nota
    sucesora de re-anclaje.
  - **H3 (RIESGO SIN REPRODUCIR)**: TEST-006 sin evidencia sobre la suite
    final (última corrida aleatoria 2026-09-13, 639 tests, pytest-randomly
    5.0.0; hoy 696 con el fixture autouse `git_isolation` que muta
    `GIT_CEILING_DIRECTORIES`/`GIT_DIR`/`GIT_WORK_TREE` con restauración en
    `finally`); `pytest_randomly` no instalado en el venv. **Resolución del
    coordinador**: no bloqueante para merge — `G-TEST-RANDOM` es opcional
    por diseño (`docs/gates.md:164`: «sin flujo automático… decisión
    pendiente»), no pertenece a los tiers commit/pr ni a los checks
    requeridos, y la puerta de merge del plan no lo incluye. Queda como
    riesgo de primera clase con receta (§4).
  - **H4 (MEJORA NO BLOQUEANTE)**: tests de inventario con oráculo congelado
    (meta-tests etiquetados con honestidad; valor contractual 48→47, no
    conductual).

### Frente C — gobernanza e integración (sin defectos en sus carriles)

- **Dependencias**: `deptry` re-ejecutado exit 0 (usado-declarado en ambas
  direcciones); `uv.lock` solo toca el extra `accept` del propio paquete
  (`pytest>=8.3`; solo 9.1.1 medido, declarado con honestidad);
  `uv lock --check --offline` exit 0; vulture con las 6 entradas nuevas
  justificadas (ADR-D-02, nombres reales en `pytest_receipt.py`) exit 0.
- **Integridad/bless**: lock 231/231 recomputado sin mismatches; commits
  post-bless solo docs; `1fc7915` solo lock+log.
- **CI/merge**: branch protection exige solo `commit-gates` (PASS en
  32d3491); historial lineal CLEAN; cero reviews exigidas por configuración.
  **No existe ningún requisito de repositorio (no humano) pendiente para
  merge sobre 32d3491.**
- **Alcance**: P03a/P03b/P03c ya versionados en la base (fuera del diff de
  la PR; P03a con contrato aprobado pero sin implementación acreditada;
  PLAN-EJECUTABLE prohíbe copiar su candidato aquí — y no está); B3-P2…P6
  fuera; E10 elegido (`1.0.0b3.dev1`) sin ejecutar (pyproject sigue
  `1.0.0-beta.2`).
- Hallazgos: **MEJORA NO BLOQUEANTE** `docs/gates.md:54` fila G-DEAD
  documenta `--min-confidence 80` y el vigente es 60 (ADR-D-02;
  **preexistente, byte-idéntico en la base 932c835**; thresholds.yaml es
  autoritativo); banda `pytest>=8.3` solo 9.1.1 verificada.

### Contradicciones resueltas por el coordinador

- B sugería H3 «antes del merge»; C verificó que G-TEST-RANDOM es opcional
  por diseño y ajeno a la puerta de merge del plan. **Resolución**:
  no bloqueante, riesgo documentado con receta; el humano puede imponerlo
  como condición previa si lo desea (§7).
- Ninguna otra contradicción material entre frentes.

## 3. Tabla de conciliación de obligaciones

| Obligación | Fuente normativa | Evidencia/decisión | Identidad | Estado |
|---|---|---|---|---|
| Mutación de fuentes AC1 (brutos r2/r2b/r3) | CONTRATO-AC1; TEST-002 | Brutos intactos + adjudicaciones y ratificaciones por ID (actas H01-H04, decisiones finales) | cortes históricos por capas | **SATISFECHA POR ADJUDICACIÓN AUTORIZADA** |
| Equivalencias y excepciones por ID | actas citadas | A1/A2/A4–A11, A12, A13, execute__28/29, casts cleanup, 5 sucesoras D-A'; excepción puntual TEST-002 B1–B5 + terminate__7 | anclas re medidas en HEAD (B) | **SATISFECHA POR ADJUDICACIÓN AUTORIZADA** |
| 928 IDs no ejecutados | ACTA-DECISIONES-FINALES §2.4 | delimitación por identidad/diff aprobada; conservan estado NO EJECUTADOS | 38 funciones portadoras | **FUERA DEL INCREMENTO, CON FUNDAMENTO** |
| Partición TEST-007 | STYLE-011/TEST-007; encargo PARTICION | 17 archivos ≤100 sitios; tests byte a byte; PX-REG; revisión A más allá del AST | 32d3491 | **SATISFECHA POR EJECUCIÓN** |
| Hooks no instrumentados | matriz D-D; ACTA §2.3 | comprobación alternativa aceptada con oráculos discriminantes; no contabilizados como mutantes | — | **SATISFECHA POR ADJUDICACIÓN AUTORIZADA** |
| Q-ACCMUT | E8; ACTA §8 | 47/47 killed, repetición fresca tras cada cambio de process.py | 8/8 anclajes re medidos en HEAD | **SATISFECHA POR EJECUCIÓN** |
| Cobertura de rama TEST-004 | TEST-004 | 314/348 = 90,23 %; prueba del denominador; líneas 91 % | transferible por identidad (sin .py tras 4217253) | **SATISFECHA POR EJECUCIÓN** |
| CRAP | STYLE-002/003 | canónico+focal conformes; suplementario delimitado (66 = 54+3+9 preexistente/trasladada, 0 nueva); frontera 12 registrada | 32d3491 | **SATISFECHA POR EJECUCIÓN** |
| DRY | STYLE-005 | 0/17 = 17/0; jscpd 0; suplementario 19 idéntico | 32d3491 | **SATISFECHA POR EJECUCIÓN** |
| Orden aleatorio TEST-006 | TEST-006 | corrida histórica (639 tests, 4 semillas); nada sobre la suite final; gate opcional por diseño | — | **PENDIENTE** (no bloqueante; §4) |
| Full PRE-BLESS | PLAN-EJECUTABLE §2 | 34 gates = 33 PASS + G-META-1 (4217253); único rojo resuelto por el bless | 4217253; insumos sin cambio posterior | **SATISFECHA POR EJECUCIÓN** |
| POST-BLESS + CI | PLAN-EJECUTABLE §2 | integrity exit 0; tier commit 21/21; commit-gates PASS en 32d3491 | 32d3491 | **SATISFECHA POR EJECUCIÓN** |
| Desviación de secuencia | encargo POST-BLESS | D-B': aceptación puntual; desviación registrada, no conforme, sin permiso general | — | **SATISFECHA POR ADJUDICACIÓN AUTORIZADA** |
| E9 + bless humano | E9/D-E9'/D-BLESS' | delta +30/0, 7 hashes + log; verificado por verifier independiente | lock@903f961 | **SATISFECHA POR EJECUCIÓN** |
| Acreditación AC1 | CONTRATO-AC1 («killed=planned, cero survived/errors/not_run») | campañas con supervivientes adjudicados; G-MUT de fuentes AC1 sin PASS | — | **PENDIENTE** (§4; no es puerta de merge) |
| Full POST-BLESS | PLAN-EJECUTABLE §2 («verificaciones finales pertinentes») | no ejecutado tras el bless; insumos de los 33 gates verdes sin cambio por identidad desde 4217253; CI verde | — | **PENDIENTE** (no bloqueante; §4) |

## 4. Acreditación AC1, beta.3 y release (respuestas separadas)

**A. ¿El incremento cumple para integrarse?** Sí — dictamen APTO PARA MERGE
(§1), condicionado a que el merge resuelva la identidad `M` sobre `32d3491`
o que todo commit intermedio sea documental y verificado como este informe.

**B. ¿AC1 cumple su contrato de acreditación?** **No.** El contrato exige
por campaña «solo baseline sano, trabajo >0, killed=planned y cero
survived/errors/not_run permite aprobar»; el incremento entregó brutos con
supervivientes **adjudicados y ratificados por decisión humana** (la vía que
el propio plan ejecutable estableció), no killed=planned. Lo falta:
(falta de ejecución) una campaña de fuentes AC1 con killed=planned, o la
decisión de mantener el cierre «por adjudicación autorizada» como estado
terminal; (falta de decisión) ninguna decisión humana ha declarado AC1
acreditado — y este informe no lo hace. Criterio de cierre: CONTRATO-AC1
§ejecución + decisión humana explícita.

**C. ¿Qué queda para el cierre integral de beta.3?** B3-P0 consolidado;
B3-P1 cadena completa (P1b/P1c según SPEC-B3-P1); B3-P2 molde; B3-P3
adopción; B3-P4 contextos; B3-P5 piloto + carril G1b (identidades de
inventario, RG13, symlinks, concurrencia, presupuesto); B3-P6 release con
calificación de SHA final; E10 (bump/lock/tag `1.0.0b3.dev1` o
`v1.0.0-beta.3`, no ejecutado); full-hardening post-merge sobre el árbol
integrado; identificación de `M` y recalificación post-merge (PLAN §2).

**D. ¿Qué queda para una versión incremental?** Elegir y autorizar el bump
incremental (E10, convención `1.0.0b3.dev1`), ejecutarlo con su lock,
calificar el SHA final (T→B→M→V del plan), CI completa sobre ese SHA,
custodia/SBOM/smokes y notas beta.2→beta.3 que separen entregado,
experimental y diferido. Las obligaciones del código publicado no se
eliminan por ser incremental.

## 5. Recetas mínimas de los pendientes (no ejecutadas aquí)

- **TEST-006 (H3)**: con autorización humana, añadir `pytest-randomly` al
  grupo dev y correr `uv run pytest -p randomly` (≥4 semillas) sobre la
  suite de 696; cualquier rojo = estado compartido (candidato primero: el
  fixture autouse `git_isolation`). ~10 min.
- **H1**: hunks de matriz con «Registro final» en A3/C3 (defectos con
  regresión permanente, ya cerrados por las actas).
- **H2**: nota sucesora de re-anclaje de A12/A13/terminate__7 a
  `1a25984b…` (con el diff byte-idéntico de las funciones mutadas como
  fundamento, ya verificado por el revisor B).
- **Full POST-BLESS**: `uv run wct gate --tier full` sobre el SHA de merge
  (o aceptación explícita de la conservación por identidad documentada
  aquí). Campañas pesadas quedan a encargo propio.
- **verdict.py:42-43 / gates.md G-DEAD / cosméticos**: incrementos
  menores con TDD propio; nada de esto es bloqueante.

## 6. Comprobaciones ejecutadas y presupuesto

Presupuesto adicional local ~400 s de 600 (lectura documental aparte):
revisor A (~250 s: sondas de identidad, AST, colección 696, lotes focales
128+55+59+68+59+5, con 3 falsos arranques ambientales por PATH de semgrep
— resueltos con `PATH=$PWD/.venv/bin:$PATH`, registrados); revisor B
(~10 s: conteos, 3 tests de humo 0,22 s, recálculo de hashes/diffs);
revisor C (~90 s: deptry, vulture, integrity check, `uv lock --check
--offline`, sondas de import); coordinador (integrity check 6,3 s, fast
gate, consultas gh). Sin full ni campañas nuevas. cwd de todas las
ejecuciones: raíz del worktree; runtime `.venv` Python 3.13.

## 7. Próxima acción humana exacta

1. Decidir el merge sobre `32d3491`: aprobar la PR y sacarla de borrador
   (acción humana; el repo no exige reviews) y fusionar — el dictamen APTO
   conserva vigencia si `M` resuelve este SHA; si se añaden commits, los
   documentales deben verificarse como este informe y los técnicos
   reabren calificación.
2. Opcional antes del merge, si se imponen como condición: receta TEST-006
   (§5) y/o full POST-BLESS (§5).
3. Post-merge: identificar `M`, recalificar el árbol integrado y abrir el
   encargo de salida incremental (E10) si se desea publicar versión.

## 8. Límites

Este informe no ejecuta merge, no saca la PR de borrador, no aprueba nada
en GitHub, no hace bump/lock/tag/release, no repara hallazgos (solo recetas),
no reabre decisiones humanas válidas, y no acredita AC1 ni cierra beta.3.
El dictamen APTO PARA MERGE es una determinación técnica de aptitud; la
decisión de integrar es humana.
