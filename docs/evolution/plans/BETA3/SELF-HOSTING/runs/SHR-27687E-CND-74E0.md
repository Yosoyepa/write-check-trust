# Registro de entrega — SH-P01 reparación R1, candidato hijo de CND-80B8

```text
task_id: SH-P01
contract_version: sh-p01/1
run_id: SHR-27687E            # local opaco, generado en esta corrida
coder_blind_id: COD-CA7A      # misma sesión/operador que el padre; conservado
candidate_id: CND-74E0        # local opaco, generado en esta corrida
parent_candidate_id: CND-80B8 # dictamen REQUIERE_CORRECCION (SHP01-R01..R05)
mode: SH-D
base_sha: 8de9107184288f1aa72c9578a14913686c47ad7c
context_digest: |
  P01-IDENTIDADES.md        sha256=7f0143ae6e6a931735914963e242e83900ca9df2bcf445f041057d1cb9b5ac99  (sin cambios desde R0)
  GHERKIN-P01.md            sha256=6f87f2ae1e0ddd5a65b4c761ced00984b92ab005899b0905d3cf9375c684b5e5  (sin cambios)
  reviews/REVIEW-CND-80B8.md sha256=b1b31112b1d7748ee31394c9ef4b8cc7af91196cccdcc2a71016d2d742b136ec
  GOBERNANZA.md / MOLDE-WCT.md / REGISTROS.md: sin cambios; hashes en el registro del padre
candidate_manifest_digest: 909e58a71e9477ccc25822823f8b3615225b68fec713cf03e22be4ccabc09cb4
  # serialización del acta §3: UTF-8, 4 líneas "sha256␣␣ruta-relativa" en su orden,
  # LF final. Calculado desde bytes, verificado por dos vías:
  # (a) sha256sum <4 archivos> | sha256sum → 909e58a7…
  # (b) sha256sum del archivo manifest.txt guardado → 909e58a7…
started_at: 2026-09-07T17:20:56-05:00
ended_at: 2026-09-07T17:39:37-05:00
timezone: UTC-05:00 (local del host)
```

## implementation_path

- **Hijo (este candidato):** `/home/jandradeu/Documents/well_code_template/build/tmp/sh-p01-CND-74E0`,
  rama nueva `codex/beta3-sh-p01-identidades-r1` (libre; creada desde `8de9107`).
- **Padre (conservado, sin tocar):** `…/build/tmp/sh-p01-CND-80B8`, rama
  `codex/beta3-sh-p01-identidades`. Verificado ANTES de reutilizar: 4 hashes
  individuales y agregado `e3a64265…` = acta §3. `git status` del padre sigue
  en 4 entradas intent-to-add; su patch archivado por el reviewer
  (`candidate-CND-80B8.patch`, sha256 `460ca37f…` según acta) no fue alterado.
- Evidencia R1 (exclusiva): `…/build/tmp/sh-p01-CND-74E0-evidence/`.
- Los cuatro archivos del padre se COPIARON al hijo y el agregado reprodujo
  `e3a64265…` desde los bytes copiados; luego se repararon solo R01/R02/R03.

## changed_files (después del último cambio)

```text
d89be6c7fa40a8f6ed21953414140c223a335a9477fdeb8145595e5c80829e5e  tools/wct/evidence/__init__.py
b7594a3c22f165655308272f680da32c8849640f3e271aa56e405b1f46dc867f  tools/wct/evidence/identities.py
1db53b953d60f19fddb3514dd04ec40882dbf98a03322f87c32ba59ae63088b1  tests/unit/test_evidence_identities.py
d3d64140477e5b67e6ee1f6967fe9cb0c4c285365b562b953043a0056b251b0e  features/wct-evidence-identities-001.feature
```

Mismo allowlist de P01 respecto de la base: 4 archivos nuevos, +787 líneas,
0 modificados/borrados. Registro nuevo: este archivo
(`runs/SHR-27687E-CND-74E0.md`). El registro del padre NO fue editado.

## Cómo se citan las discrepancias del padre (R04)

- **Digest agregado:** en R0 declaré `de207e0d…` calculando
  `sha256sum <rutas ABSOLUTAS del worktree> | sha256sum`; la fórmula del acta
  usa rutas relativas → `e3a64265…`. Causa identificada: rutas absolutas en la
  tubería, no alteración de archivos (los 4 hashes individuales coincidían).
- **«401 passed ×4»:** atribución injustificada. Evidencia real de R0: una
  corrida ambiental FALLIDA (1 failed + 24 skips por venv sin grupo quality),
  una corrida completa 401 en 90.31s (`full-suite.txt`), y dos ejecuciones de
  G-TEST dentro de los tiers (selección unit/integration sin property: 399,
  no 401). En este registro cada corrida va separada con su selección real.
- **Nodeids I19/I20:** en R0 escribí `[expected-string]`; el orden real es
  `[string-expected]` (contenedor-elemento primero). Aquí se transcriben desde
  `focal-run-final.txt`, no de memoria.
- **Statements 53 vs 54:** el 53 correspondía a la medición previa a añadir
  `__all__` al final de la secuencia; el candidato final del padre medía 54.
  Aquí cada medición queda vinculada a su snapshot.

## Preflight del entorno (sin sync, sin instalación)

- Python **3.13.14**; pytest **9.1.1**; mypy **2.3.1**; ruff **0.16.4**;
  vulture **2.16**; crap4py presente en `bin/` (pin `crap4py>=0.1.1` en
  pyproject; lock del worktree = lock de la base, sin regenerar). El acta
  midió con crap4py 0.1.1 y Python 3.13.14 — mismo entorno base.
- El venv preparado del PADRE (`build/tmp/sh-p01-CND-80B8/.venv`, ya con todos
  los grupos declarados desde R0) se REUTILIZA solo-lectura:
  `UV_PROJECT_ENVIRONMENT=<venv del padre>` + `PYTHONPATH=<raíz del hijo>` +
  `uv run --no-sync` con cwd en el hijo. **No hubo sync, instalación, upgrade
  ni cambio de intérprete** → no se requirió autorización de sync.
- Resolución verificada por sonda: desde cwd externo, `import
  tools.wct.evidence.identities` resuelve a
  `/…/build/tmp/sh-p01-CND-74E0/tools/wct/evidence/identities.py` (PYTHONPATH
  precede al site-packages del venv). Los gates corren con cwd=hijo; G-TEST
  ejecuta pytest sobre el árbol del hijo.

## attempts

1. **Validación del padre:** 4 hashes + agregado `e3a64265…` ✓; copia al hijo
   y re-cálculo del agregado desde los bytes copiados ✓.
2. **R01 (refactor de complejidad, comportamiento conservado):**
   `_inventory_findings` (CRAP 10) → partición cohesiva en `_non_empty_counts`,
   `_empty_findings`, `_duplicate_findings`, `_gap_findings`,
   `_observed_findings`; `_resolved_status` (CRAP 7) → tabla de precedencia
   `_STATUS_PRECEDENCE` que transcribe §4 en orden (no estructura opaca: cada
   entrada es una regla del contrato). Los 54 tests del padre: 54 passed.
3. **R02 (I21b):** test nuevo con expected literal: E=(B,A,B,A),
   C=(Z,Y,A,B,Z,Y,""), X=(D,C,D,C) → 2+ IDs duplicados en CADA inventario,
   múltiples missing (A,B) y unexpected (C,D; Y,Z), salida completa en orden y
   una segunda llamada con entradas permutadas que debe dar el mismo
   resultado. Verde contra el original (su lógica ya ordena bien), como
   admite el encargo.
4. **Variante A (rojo semántico de R02, se revierte):** `sorted(counts.items(),
   reverse=True)` en duplicados → **1 failed exacto:
   test_i21b_canonical_order_with_repeated_ids**, 57 passed. Revertida: 58
   passed. (El escape documentado en el acta — 54 verdes con la variante — ya
   no se reproduce.)
5. **R03 (binding):** `_bound_rows` valida, ANTES del dict: (a) exactamente 1
   Scenario Outline, (b) cardinalidad 18, (c) unicidad de casos, (d)
   pertenencia exacta a la tabla. Positivo reescrito sobre el helper. Tres
   negativos con el PARSER PRODUCTIVO real sobre features temporales en
   tmp_path: fila I01 duplicada (19 filas → cardinalidad), duplicado a igual
   cardinalidad (I02→I01 → unicidad), Scenario Outline extra. Feature
   aprobado: intacto y verbatim; pasa.
   - Nota de intento: el primer negativo usaba `match="repetido"` y falló
     porque con 19 filas dispara PRIMERO la cardinalidad («se esperaban 18
     filas, hay 19»). Se partió en dos negativos para ejercitar ambas
     validaciones por separado. No fue flake ni defecto de producto.
6. **Variante B (rojo semántico de R03, se revierte):** cuerpo legacy
   (`scenarios[0]` + dict directo, sin validaciones) → **3 failed exactos**
   (los tres negativos), 55 passed — el positivo sigue pasando con el binding
   legacy, que es el escape del acta. Revertida: 58 passed.
7. `ruff format` sobre los 2 archivos python nuevos (reflow sin cambio
   semántico; 58 passed después).

## checks (cada corrida separada; cwd del hijo salvo indicación; outputs en
`sh-p01-CND-74E0-evidence/`)

| # | Comando (argv esencial) | exit | Resultado real / archivo |
|---|---|---|---|
| 1 | `uv run --no-sync pytest tests/unit/test_evidence_identities.py -q` | 0 | **58 passed** (54 padre + I21b + 3 negativos) |
| 2 | `uv run --no-sync pytest tests/unit/test_evidence_identities.py -v --no-header` | 0 | 58 PASSED; `focal-run-final.txt` (nodeids reales) |
| 3 | focal: `pytest tests/unit/test_evidence_identities.py -q -m "not property" --cov=tools.wct.evidence --cov-branch --cov-report=lcov:<EV>/focal.lcov --cov-report=term-missing`; COVERAGE_FILE exclusivo | 0 | **59 statements, 18 branches, 100%**, 58 passed; `focal.lcov` |
| 4 | `uv run --no-sync crap4py tools/wct/evidence --lcov <EV>/focal.lcov --max-crap 6` | 0 | `crap.txt`: máx **5.0** (compare_identities); las 9 funciones ≤ 5.0. Antes: 10/7 |
| 5 | `uv run --no-sync wct dry tools/wct/evidence/identities.py tools/wct/evidence/__init__.py` | 0 | `dry-scope.txt`: candidates [], errors [], units 9 |
| 6 | global: `uv run --no-sync pytest --cov --cov-branch --cov-report=lcov:build/coverage/lcov.info -q -m "not property"` | 0 | `global-coverage.txt`: **404 passed, 1 deselected** (property), 113.61s; lcov sha256 `cdccbe33f605aae94ca052d56f355c4effc3e19f80d6a12f56d1d75363ae45e0` |
| 7 | `uv run --no-sync wct ratchet check` | 0 | `ratchet-tolerant.txt`: «Todos los ratchets se mantienen.» |
| 8 | `uv run --no-sync wct ratchet check --require coverage-total` | 0 | `ratchet-require.txt`: «medidas 1 de 1 exigibles» — productor: pytest --cov con source src+tools/wct (pyproject), métrica: piso de `governance/baselines/coverage-total.json` sobre el LCOV global recién producido (run 6) |
| 9 | `uv run --no-sync pytest --collect-only -q` | 0 | **405 collected** (`collect-all.txt`) |
| 10 | `uv run --no-sync pytest --collect-only -q tests/unit tests/integration -m "not property"` | 0 | **403 collected** (`collect-gtest.txt`) — selección real del G-TEST |
| 11 | `uv run --no-sync pytest -q tests/unit tests/integration -m "not property"` | 0 | `gtest-selection.txt`: **403 passed**, 90.83s |
| 12 | `uv run --no-sync pytest -q` | 0 | `full-suite.txt`: **405 passed**, 96.27s (incluye property) |
| 13 | `uv run --no-sync wct gate --tier fast` | 0 | `gate-fast.txt`: 7/7 PASS |
| 14 | `uv run --no-sync wct gate --tier commit` | 1 | `gate-commit.txt`: 20/21 PASS; único FAIL **G-META-1** (drift pre-bless) |
| 15 | `uv run --no-sync wct integrity check` | 1 | `integrity.txt`: drift EXACTAMENTE `tools/wct/evidence/__init__.py` + `tools/wct/evidence/identities.py` (las dos rutas de producto contratadas); sin rutas ajenas |
| 16 | `git diff --check` (tras `git add -N`) | 0 | limpio |

## case_bindings (extraídos de `focal-run-final.txt`, no reconstruidos)

| Caso | nodeid real (prefijo `tests/unit/test_evidence_identities.py::`) | Resultado |
|---|---|---|
| I01–I18 | `test_identity_table_row[Ixx]` (18) | PASSED ×18 |
| I19 | `test_i19_non_tuple_container_raises[<contenedor>-<campo>]` — p. ej. `[string-expected]`, `[string-collected]`, `[string-executed]` (21: 7 contenedores × 3 campos) | PASSED ×21 |
| I20 | `test_i20_non_string_element_raises[<elemento>-<campo>]` — p. ej. `[none-expected]` (12: 4 elementos × 3 campos) | PASSED ×12 |
| I21 | `test_i21_immutable_deterministic_canonical` | PASSED |
| I21b (R1) | `test_i21b_canonical_order_with_repeated_ids` | PASSED |
| I22 | `test_i22_identity_opacity` | PASSED |
| binding positivo | `test_feature_rows_bind_to_literal_table` | PASSED |
| negativo cardinalidad | `test_binding_rejects_duplicated_case` | PASSED |
| negativo unicidad | `test_binding_rejects_duplicate_ids_same_cardinality` | PASSED |
| negativo escenario extra | `test_binding_rejects_extra_scenario` | PASSED |

Total focal: 58. Matriz completa con orden y tiempos: `focal-run-final.txt`.

## red_evidence (calibración manual; NO mutmut/G-MUT/G-ACCEPT-MUT)

- Variante A (orden descendente de duplicados en producto): 1 failed
  (I21b) / 57 passed → el escape R02 del acta queda discriminado.
- Variante B (binding legacy en test): 3 failed (negativos) / 55 passed → el
  escape R03 queda discriminado; el positivo pasa con legacy, fiel al escape.
- Controles válidos: el candidato (y el original) pasan las 58; ninguna
  variante rompe el positivo del feature aprobado.
- NO califiqué una segunda estrategia de conteo alternativa: no se afirma.
- Ambas variantes se revirtieron; los hashes finales no las contienen.

## deviations

1. **Reutilización del venv del padre** con `UV_PROJECT_ENVIRONMENT` +
   `PYTHONPATH`→hijo y `--no-sync`: el encargo ordenaba pedir sync si el
   entorno nuevo lo necesitaba. La disposición no requirió sync, instalación
   ni cambio de intérprete; se declara completa y verificada por sonda porque
   de ella depende que TODOS los controles midieran los bytes del hijo.
2. `git add -N` (intent-to-add) local en el worktree hijo para `git diff
   --check` y el diff de entrega; no es commit.
3. IDs run/candidato generados localmente (opacos); coder_blind_id conservado.
4. `ruff format` reflowed ambos archivos python (sin cambio semántico).

## unverified

- Tiers `full`/`pr` completos (G-MUT/mutmut, G-COV-DIFF, G-ACCEPT-MUT,
  G-REDTEAM, G-SBOM, G-AUDIT, G-SAST-SEMGREP, G-HOOKS-WIRED): no corridos.
  El G-CRAP histórico de src NO acredita esta pieza; la medición CRAP de este
  registro es directa sobre `tools/wct/evidence` (run 4).
- Orden aleatorio de tests (G-TEST-RANDOM): no corrido (sin tier local).
- Aceptación ensamblada con handlers: pieza P06; fuera de alcance.
- Segunda implementación independiente completa del comparador: no construida.
- Coste/tokens: unavailable (custodia del operador).

## incidents

- Ningún timeout/cancelación/reintento. El fallo del primer negativo
  (mensaje de cardinalidad) fue un ajuste de diseño del test, registrado en
  attempts §5, no un incidente de ejecución.

## assistance

Diagnóstico y criterios de reparación del arquitecto vía
`reviews/REVIEW-CND-80B8.md` (R01–R05, variantes y fórmula de manifiesto).
Ningún fix de producto escrito por el arquitecto; implementación y tests de
esta reparación: coder.

## custody / git / next

```text
usage_custody: recibos privados con el operador; telemetría unavailable
git_actions: no commit / no push / no PR / no bless
next_action: revisión ciega independiente sobre ESTOS bytes (CND-74E0);
  conservar padre CND-80B8, su acta y su patch; F01–F06 siguen sin implementar
```
